@using CorujasMimo.LojaVirtual.Web.HtmlHelpers
@model CorujasMimo.LojaVirtual.Web.Models.ProdutosViewModel


@foreach (var p in Model.Produtos)
{
    @Html.Partial("ProdutoDetalhes", p)
}
<div align="right">
    @Html.PageLinks(Model.Paginacao, x => Url.Action("ListaProdutos", new { pagina = x }))
</div>